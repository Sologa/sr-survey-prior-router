interface:
  display_name: "SR/Survey Prior Router"
  short_description: "Explicit-only router for prepared SR/survey prior references"

policy:
  allow_implicit_invocation: false

capabilities:
  - Route explicit requests for prepared SR/survey prior references.
  - Locate the smallest relevant source inventory, corpus index, or source card.
  - Keep broad synthesis, source-integrity, and provenance boundaries visible.
